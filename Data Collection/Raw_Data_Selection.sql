use weather;
select distinct * from humidity order by timestamp;
select distinct * from rainfall order by timestamp;
select distinct * from temperature order by timestamp;
select distinct * from winddirection order by timestamp;
select distinct * from windspeed order by timestamp;